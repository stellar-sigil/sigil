[package]
name = "corpus-no-require-auth"
description = "SIG-001 corpus pair: a transfer that never demands authorization, and its fix"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
soroban-sdk = "27"

[dev-dependencies]
soroban-sdk = { version = "27", features = ["testutils"] }
sigil-spec = { path = "../../crates/sigil-spec" }
sigil-test = { path = "../../crates/sigil-test" }
