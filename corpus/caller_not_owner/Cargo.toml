[package]
name = "corpus-caller-not-owner"
description = "SIG-002 corpus pair: a vault that authorizes the caller instead of the account whose funds move"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
soroban-sdk = "27"

[dev-dependencies]
soroban-sdk = { version = "27", features = ["testutils"] }
sigil-spec = { path = "../../crates/sigil-spec" }
sigil-test = { path = "../../crates/sigil-test" }
