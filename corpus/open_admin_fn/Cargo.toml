[package]
name = "corpus-open-admin-fn"
description = "SIG-005 corpus pair: an admin gate that authorizes whoever asks"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
soroban-sdk = "27"

[dev-dependencies]
soroban-sdk = { version = "27", features = ["testutils"] }
sigil-spec = { path = "../../crates/sigil-spec" }
sigil-test = { path = "../../crates/sigil-test" }
