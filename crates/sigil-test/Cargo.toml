[package]
name = "sigil-test"
description = "Assert a Soroban contract's authorization surface against its sigil.toml"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
sigil-spec = { path = "../sigil-spec", version = "0.1.0" }
soroban-sdk = { version = "27", features = ["testutils"] }

[dev-dependencies]
soroban-sdk = { version = "27", features = ["testutils"] }
